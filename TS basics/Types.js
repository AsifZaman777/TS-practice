let userName = 'John';
userName = 33;


//the problem is that we are trying to assign a number to a variable that is of type string.
//This is not allowed in TypeScript, because it is a statically typed language.


console.log(userName); 